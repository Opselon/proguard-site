# Base URL for serving images from a CDN. Leave empty to serve from the worker.
IMAGE_CDN_BASE=""

# Default locale ('en' or 'fa')
DEFAULT_LOCALE="fa"

# Default theme ('light' or 'dark')
THEME_DEFAULT="light"

# Enable development aids like outlines for lazy-loaded elements
DEV_DEBUG_MODE="false"